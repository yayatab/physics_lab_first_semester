import numpy


# def mean_value(*args):
#     """args = ilst of tuples(x, p(x)"""
#     l = list(args)
#     return sum([x(0) * x(1) for x in l])


def expected_value(array)
    return numpy.mean(array)


def expected_value_weights(array, weights):
    return numpy.average(array, weights)
